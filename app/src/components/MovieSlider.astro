---
export interface Props {
        url?: string,
        n? : string,
        uId? : string,
        title? :string,
    }

const n2 = 20
const n_rec =[]
Array.from({ length: n2}, (x, i) => {n_rec.push(i)})

---

